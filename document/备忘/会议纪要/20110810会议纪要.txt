8/10 LPP数据库优化讨论会

待后续讨论确认问题：
1.数据库表名称的前缀命名方式
2.订单操作检查用户权限问题：是否只校验一方权限？
3.一次性采购的物料在系统怎么设置物料代码？

会议定下的规则：
1.所有的描述字段命名成：Desc1、类型为Varchart(100)，有特殊需求的企业例外。
2.Code字段类型长度默认Varchart(20)，字段Value默认为Varchart(50)，字段Seq默认Int型。
3.CodeMstr，ACC_Role表增加Type字段来区分用户级与系统级。
4.“是”与“否”类型的字段定义Bit型。
